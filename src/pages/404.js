import React from 'react';

// markup
const NotFoundPage = () => {
	return (
		<main className="flex items-center justify-center h-screen">
			<h1 className="text-primary text-4xl">404 | Pagina no encontrada</h1>
		</main>
	);
};

export default NotFoundPage;
